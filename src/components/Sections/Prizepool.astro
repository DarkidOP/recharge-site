<section id="prizepool" class="prizepool-sec">
    <div class="line"></div>
    <div class="text-wrapper container">
        <h2 class="title">Prizepool</h2>
        <h1  class="accent-clr">Total prize of BDT 20 LACKS</h1>
        <h2 class="desc">AND AN OPPORTUNITY <span class="accent-clr">TO GET YOUR FACE ON THE Recharge BOTTLE.</span></h2>
    </div>
    <div class="overlay"></div>
    <div class="backdrop"></div>
</section>
<style>
    .prizepool-sec{
        width: 100%;
        min-height: 800px;
        display: grid;
        place-items: center;
        grid-template-areas: 'stack';
        position: relative;
        margin-top: 1rem;
    }
    .line{
        width: 80%;
        height: 3px;
        background-color: var(--clr-accent-1);
        position: absolute;
        top: -1.5px;
    }
    .backdrop{
        width: 100%;
        height: 100%;
        opacity: 0.35;
        z-index: -1;
        background: url('/assets/prizepool-bg.png');
        background-position: center;
    }
    .prizepool-sec > *{
        grid-area: stack;
    }
    .overlay{
        position: absolute;
        bottom: 0;
        width: 100%;
        height: 90%;
        background: linear-gradient(180deg, rgba(var(--clr-dark-1-rgba), 0) 0%, var(--clr-dark-0) 100%);
    }
    .text-wrapper{
        text-transform: uppercase;
        z-index: 2;
    }
    h1{
        font-size: clamp(4rem, 10vw, 6.8rem);
        transform: skew(-0.014turn, 0);
   
        /* letter-spacing: 0.03em; */
        line-height: 1em;
    }
    h2{
        font-size: clamp(2.5rem, 8vw, 4rem);
        letter-spacing: 0.01em;
        line-height: 1em;
    }
    .desc{
        opacity: 0.65;
    }
</style>